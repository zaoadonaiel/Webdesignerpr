"""WebDesignerPR static site generator. Standard library only — no dependencies.

NOTE: this package must not be called `site` — that shadows a Python
standard-library module and breaks the interpreter's startup imports.
"""
